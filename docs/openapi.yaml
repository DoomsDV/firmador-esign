# Índice OpenAPI — ya no es una spec ejecutable
#
# La API se dividió en tres contratos independientes.
# Ver el índice canónico:
#
#   docs/openapi/README.md
#
# Specs:
#   docs/openapi-emision.yaml   — integradores (sk_test_/sk_prod_)
#   docs/openapi-panel.yaml     — panel web (JWT)
#   docs/openapi-internal.yaml  — Go↔ORDS privado (X-Service-Token)
#
# No importar este archivo en Swagger UI / Redoc.
